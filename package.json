{
  "name": "crudzaso-habitflow",
  "version": "1.0.0",
  "description": "HabitFlow - MatchFlow Job/Candidate Platform",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db/db.json --port 3000",
    "dev": "json-server --watch db/db.json --port 3000",
    "server": "json-server --watch db/db.json --port 3000"
  },
  "keywords": [
    "jobs",
    "candidates",
    "matching",
    "platform"
  ],
  "author": "CRUDZASO",
  "license": "MIT",
  "dependencies": {
    "json-server": "^1.0.0-beta.5"
  },
  "devDependencies": {}
}
